# openv2b overnight verification and build report — 2026-07-29/30

Overnight campaign on the clean-room open-source V2B simulator at `/home/rishav/openv2b`.
Source of truth is this Markdown file; `OVERNIGHT_REPORT.html` is generated by
`tools/md2html.py` (never hand-edited). No critical blockers were hit; nothing required a STOP.

## Scope agreed with Rishav

1. Solver layer: 3-tier Rust `MilpBackend` trait, no PuLP/CVXPY (Python model-build time and
   file+CLI handoff dominate repeated MPC re-solves).
2. Verification-first: plan, independent review, execute, audit; MPC only if clean.
3. Session persistence implemented tonight. 4. ACN-Sim V2B extension: plan only. 5. STOP on criticals.

## Bottom line

- **Everything planned for the night landed.** Persistence, TOU/demand billing, the full
  verification apparatus, two independent Opus review rounds (39 findings total, all triaged),
  a mutation-testing round, the 3-tier solver layer, and a working MPC policy.
- **Final state: 58 Rust tests green** (invariants, hand-computed goldens, 1200-run randomized
  sweep with coverage assertions, mutation-kill suite, MPC), `clippy -D warnings` and `rustfmt`
  clean on both feature sets, **19 month-scale runs all referee-verified and two-process
  deterministic**.
- **The independent referee now re-simulates every heuristic policy from scratch in Python and
  must agree with the Rust engine slot-exactly across 2880 slots.** It does, on all 18
  heuristic runs, including banking, force-charge, the V2B discharge overlay, asymmetric
  efficiencies, and a binding site cap. The referee runs in CI.
- **MPC works and is fast**: the full month (2880 receding-horizon LP re-solves, in-process
  HiGHS) takes **1.7 seconds** end to end and beats the best heuristic by another **$1286**.
  The LP-file CLI backend was verified **bill-identical against local CPLEX 22.1**.

## Month campaign results (30 days, 8 vehicle archetypes, 13 DR events, lossless dataset)

| policy | total $ | energy $ | demand $ | DR penalty $ | DR incentive $ | mean net in DR windows kW |
|---|---|---|---|---|---|---|
| idle (no EVs) | 7532.20 | 5125.50 | 700.20 | 1774.50 | 68.00 | 56.3 |
| uncontrolled | 9525.65 | 5621.20 | 991.95 | 2980.50 | 68.00 | 65.0 |
| edf / llf | 8318.84 | 5620.39 | 991.95 | 1774.50 | 68.00 | 56.3 |
| edf-v2b / llf-v2b | 4318.88 | 5491.43 | 991.95 | 79.50 | 2244.00 | 44.2 |
| **mpc (HiGHS)** | **3032.53** | 4795.53 | 700.20 | 12.00 | 2475.20 | — |

- The idle bill's DR terms were verified by closed-form hand arithmetic ($1774.50 penalty =
  9 evening events x $180 + $12 + $90 + $52.50 boundary events; $68 incentive = the single
  honored final-slot window). They match exactly.
- V2B banking + discharge shaves the mean in-window net load by 12.15 kW vs idle (M30), turns
  9 violated windows into honored ones, and earns $2244 of incentives.
- MPC additionally arbitrages TOU prices (its energy cost is below even idle's: discharge
  offsets building load in expensive slots) and keeps the peak-TOU demand at the idle level.
- Caveat worth knowing: banking raises the *off-peak* site peak (96 kW vs 85). Costless under
  this tariff (facilities demand rate is 0) but a nonzero facilities rate would trade off.
- The persistence-off twin shows why persistence matters: the surplus donor re-arrives
  pre-charged daily (free energy from outside the meter) and the V2B bill drops another $300+
  for spurious reasons. Full tables: `reports/month_results.md`.

## Verification story (what makes these numbers trustworthy)

Layered, each layer independently authored:

1. **Rust invariant tests**: energy conservation under asymmetric efficiencies, SoC bounds,
   no-export, per-port + aggregate + site-cap power limits, bill identities, DR window
   boundary semantics, persistence chain identity + clamp accounting, determinism
   (byte-identical, two-process SHA-256, CSV-row-permutation invariance), adversarial-policy
   safety (NaN/inf/huge/duplicate setpoints), charger queueing/exclusivity.
2. **Hand-computed goldens**: every dollar derived on paper in test comments.
3. **Randomized property sweep**: 200 seeded scenarios x 6 policies; all invariants asserted
   per run; corpus-coverage counters prove the sweep actually hits discharge, contention,
   binding caps, target misses, chain clamps, and DR overflow.
4. **Hand-written DR covered-slot table**: the (start,end] convention anchored by a literal
   slot list written from wall-clock arithmetic, independent of the code.
5. **Independent Python referee** (`tools/referee.py`, stdlib only, in CI): re-derives every
   billing metric from raw outputs, checks all physics bounds from the trace, verifies the
   persistence chain, and **re-simulates all six heuristic policies from scratch**, requiring
   slot-exact agreement. Two implementations of the same spec, written separately, agreeing
   to 1e-9 across 51,840 slot-records per dataset.
6. **Mutation testing**: 17 deliberate bugs (window-convention flip, dropped efficiency terms,
   deleted no-export guard, swapped demand components, dropped depletion, reordered event
   processing, broken incentive gate, ...) — after closing the survivors, **every mutant is
   killed by a named test**. Spot-verified after the fix round.

## Review rounds (independent Opus agents) and what they caught

**R1 plan review: 25 findings (5 critical), all fixed with regression tests.** Highlights:
the V2B discharge budget could sacrifice departure targets when a DR window abutted departure
(fixed: provably-safe surplus-only budget); out-of-horizon DR events paid full incentives and
took the bill negative; overlapping DR events double-billed; NaN setpoints were applied twice
producing negative charge columns; the site cap was advisory; V2B donors could be stranded on
unidirectional ports; the persistence clamp silently manufactured energy (now reported per
session as `chain_clamped_kwh`).

**Referee catch during execution**: on the lossy dataset the discharge budget was converted to
building-side power without the discharge efficiency, dipping 0.197 kWh below the reserve —
exactly the class of shared-convention bug the independent referee exists to catch.

**R2 correctness audit: 18 findings (2 critical), all fixed or resolved.** The big one: the
engine rationed scarce headroom in CSV row order instead of the policy's emission order,
violating the spec, breaking row-permutation invariance under a binding site cap, and
disagreeing with the referee — invisible because no shipped dataset set a site cap. Fixed with
canonical session ordering + emission-order arbitration; the lossy month dataset now carries a
binding 75 kW cap. Also fixed: NaN inputs sailed through validation and were silently swallowed
into an under-stated bill (now rejected, "validated, never repaired" is true); EDF/LLF starved
trivially feasible sessions when building load exceeded the firm level (now a force-charge
fallback, mirroring standard practice: service guarantee outranks the window, penalty is paid);
the month dataset's V2B donor ran dry after day 3 (now the V2B variants *bank* off-peak, which
is also what makes the V2B numbers above meaningful); plus validation gaps (arrival below
floor, negative targets, duplicate charger ids, duplicate series rows) and referee robustness
fixes.

**R2 mutation audit: 8/14 initial kill rate, 6 survivors + 2 bonus findings, every survivor
closed** with a test empirically verified to kill its mutant (the audit itself verified each
proposed test kills). The recurring failure mode it named: tests that pass "for the wrong
reason" because their geometry never makes the guarded path bind. All named instances fixed.

## Solver layer (per the evening's decision)

`milp::MilpBackend` trait; formulation code written once:

| tier | status | per-solve overhead |
|---|---|---|
| HiGHS in-process (`--features solver-highs`, MIT) | implemented + tested | microseconds |
| LP-file + CLI (any solver: CPLEX, Xpress, Hexaly...) | implemented; **CPLEX 22.1 verified bill-identical to HiGHS** | ~100-500 ms |
| Gurobi in-process (`grb` crate) | designed, not yet implemented (no local license to test against) | microseconds (planned) |

MPC formulation: pure LP (split charge/discharge variables, SoC recursion with efficiencies,
reachability terminal condition with penalized shortfall, no-export + site-cap aggregate
constraints, both demand components, per-event DR overflow pricing, battery-wear cost).
Honest information set: connected sessions + public series only, no future arrivals. The
engine clamps everything, so a wrong solve can cost money but never break physics (tested).
Determinism: threads and seeds pinned in both backends; LP emission is byte-stable.

## ACN-Sim V2B extension plan (item 3)

Full plan in `docs/ACNSIM_V2B_PLAN.md`, from a source-level study of acnportal v0.3.3.
Recommendation: a **plugin package** (`acnportal-v2b`) over unmodified acnportal, not a fork
(the credibility claim stays auditable: "V1G core is bit-identical to released Caltech code;
V2B is an additive layer"), and not upstream PRs (master has had no merge since 2023-11).
Verified file-level change list (11 components, ~7 S + 6 M, est. 2-3 weeks): bidirectional
EVSE/battery/EV subclasses, a network override for the no-export guard, a V2B interface with
building load + DR accessors, series tariffs, mirrored policies, a scenario bridge reading
openv2b directories, and an 8-experiment cross-validation matrix (X1-X8) from unit-convention
parity up to full billing parity and an information-loss canary. Known ACN-side traps
documented (a linear-feasibility path that under-constrains signed schedules, stock algorithms
clamping negative pilots, EV-only peak accounting).

## Known gaps (documented, not hidden)

- Multi-building, negotiation layer, tariff ratchets/minimum-demand clauses: roadmap.
- Gurobi in-process backend: designed only (untestable here without a license).
- MPC-vs-oracle parity suites (deficit AND surplus regimes + drift canary): next session;
  the SPEC records the regime-dependence lesson so it does not get lost.
- The referee's billing checks over engine output share the engine's net-load series by
  construction; the genuinely independent anchors are the six re-simulated policies, the input
  echoes, and the hand-written DR table (all in place).
- `output.rs`/CLI have no direct unit tests; the CI referee steps cover the output contract
  end-to-end instead.

## Repo state

`/home/rishav/openv2b`: 58 tests, fmt + clippy clean (both feature sets), CI with referee
steps, docs (SPEC, SOLVER_DESIGN, VERIFICATION_PLAN, VALIDATION, ACNSIM_V2B_PLAN,
INPUT_FORMAT, PROVENANCE, ROADMAP), examples (one_day, one_month, one_month_lossy), tools
(referee, verification driver, md2html). Still uncommitted by design: the first commit should
carry your identity and DCO sign-off (`git commit -s`).
